import { type FC, type ReactElement } from "react";
// import { ClientForm } from "@/modules/createClient/components/ClientForm";

export const CreateClient: FC = (): ReactElement => {
  return (
    <div>
      <h1>Create Client</h1>
    </div>
  );
};
