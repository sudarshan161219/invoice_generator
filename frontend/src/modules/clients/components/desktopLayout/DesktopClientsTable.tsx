import type { FC, ReactElement } from "react";
import { Tabs } from "../Tabs/Tabs";

export const DesktopClientsTable: FC = (): ReactElement => {
  return (
    <div>
     <h1></h1>
    </div>
  );
};
